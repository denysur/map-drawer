# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
on: pull_request
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run build
        env:
          VITE_MAP_API_KEY: ${{ secrets.VITE_MAP_API_KEY }}
          VITE_FIREBASE_APP_API_KEY: ${{ secrets.VITE_FIREBASE_APP_API_KEY }}
          VITE_FIREBASE_APP_AUTH_DOMAIN: ${{ secrets.VITE_FIREBASE_APP_AUTH_DOMAIN }}
          VITE_FIREBASE_APP_PROJECT_ID: ${{ secrets.VITE_FIREBASE_APP_PROJECT_ID }}
          VITE_FIREBASE_APP_STORAGE_BUCKET: ${{ secrets.VITE_FIREBASE_APP_STORAGE_BUCKET }}
          VITE_FIREBASE_APP_MESSANGING_SENDER_ID: ${{ secrets.VITE_FIREBASE_APP_MESSANGING_SENDER_ID }}
          VITE_FIREBASE_APP_ID: ${{ secrets.VITE_FIREBASE_APP_ID }}
          VITE_FIREBASE_APP_MEASURMENT_ID: ${{ secrets.VITE_FIREBASE_APP_MEASURMENT_ID }}
          VITE_MAP_LIGHT_STYLE_URL: ${{ secrets.VITE_MAP_LIGHT_STYLE_URL }}
          VITE_MAP_DARK_STYLE_URL: ${{ secrets.VITE_MAP_DARK_STYLE_URL }}

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_MAP_DRAWER_BA1D1 }}
          projectId: map-drawer-ba1d1
          expires: 3d
